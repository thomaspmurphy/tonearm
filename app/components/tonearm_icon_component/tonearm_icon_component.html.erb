<div class="relative">
  <svg
    class="w-6 h-6 transition-transform duration-300 ease-in-out origin-bottom-left text-white"
    style="transform: rotate(<%= rotation_angle %>deg)"
    viewBox="0 0 24 24"
    fill="none"
    xmlns="http://www.w3.org/2000/svg">
    <!-- Tonearm base -->
    <circle cx="4" cy="20" r="2" fill="currentColor"/>
    <!-- Tonearm -->
    <path
      d="M4 20L18 6"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"/>
    <!-- Cartridge/stylus -->
    <path
      d="M18 6L20 4"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"/>
  </svg>
</div>
